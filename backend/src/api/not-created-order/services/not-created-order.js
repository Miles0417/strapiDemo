'use strict';

/**
 * not-created-order service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::not-created-order.not-created-order');
