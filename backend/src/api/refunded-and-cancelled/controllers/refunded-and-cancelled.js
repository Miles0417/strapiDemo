'use strict';

/**
 * refunded-and-cancelled controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::refunded-and-cancelled.refunded-and-cancelled');
