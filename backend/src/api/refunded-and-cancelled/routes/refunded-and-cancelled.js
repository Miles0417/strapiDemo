'use strict';

/**
 * refunded-and-cancelled router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::refunded-and-cancelled.refunded-and-cancelled');
